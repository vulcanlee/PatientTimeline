# FHIR Patient Timeline

React + TypeScript app for loading patient data from a FHIR server and rendering a timeline grouped by encounter.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

## Build

```bash
npm run build
npm run lint
```
